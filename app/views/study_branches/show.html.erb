<div class="p-4 bg-white shadow-md rounded-lg max-w-md mx-auto">

  <h1 class="text-2xl font-semibold text-gray-800 mb-2">
    <%= @study_branch.name %>
  </h1>

  <p class="text-gray-600 mb-4">
    <%= @study_branch.course_id %>
  </p>


  <div class="mt-6 flex space-x-4">
    <%= link_to "Editar", edit_study_branch_path(@study_branch), class: "border-2 p-2 rounded" %>
    <%= link_to "Eliminar", study_branch_path(@study_branch), data: {
      turbo_method: :delete, turbo_confirm: "¿Estas seguro?"
      }, class: "text-red-600 border-2 p-2 rounded" %>
  </div>
</div>