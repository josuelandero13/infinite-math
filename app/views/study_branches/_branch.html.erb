<div class="mb-4 border-b pb-4">
  <%= link_to study_branch_path(branch) do %>
    <span class="block text-lg font-semibold text-gray-700">
      <%= branch&.name %>
    </span>
  <% end %>

  <span class="block text-sm text-gray-500">
    <%= "#{t(".course_label")}: #{branch.course.name}" %>
  </span>
</div>
